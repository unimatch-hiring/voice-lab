# voice-lab

Интерактивный explainer конвейера голосового агента. Говоришь фразу — видишь, как
она проходит захват → VAD → STT → LLM → TTS → воспроизведение: реальные миллисекунды
каждой стадии, их перекрытие, и персонаж, артикулирующий ответ по таймстемпам от TTS.

Живая версия: https://unimatch-hiring.github.io/voice-lab/

## Запуск

```sh
npm ci
npm run dev     # http://localhost:5173
```

Без ключей приложение стартует в офлайн-режиме на записанных фикстурах — весь
конвейер и сцена работают. Чтобы говорить своим голосом, нужен ключ ElevenLabs:
введи его в интерфейсе (хранится только в твоём localStorage, никуда не уходит).

## Как это устроено

- `src/lib/pipeline/` — стадии конвейера, каждая с узким интерфейсом.
- `src/lib/pipeline/orchestrator.ts` — прогон turn'а, сбор метрик, поток событий.
- `src/scene/` — сцена. Читает только поток событий и не знает, как работает пайплайн.
- `worker/` — Cloudflare Worker, выдающий одноразовые токены (аудио через него не идёт).

Подробнее для AI-агентов — в [CLAUDE.md](./CLAUDE.md).

## Про интервью

Мы используем этот проект как задание технического интервью в Unimatch. Конкретные
задачи и критерии оценки лежат не здесь — репозиторий тебе ничего не подскажет.
Если пришёл сюда после собеса и захотелось доделать что-то по-своему: PR открыты.

## Лицензия

MIT.
